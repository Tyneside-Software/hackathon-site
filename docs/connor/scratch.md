# Connor — scratch

Empty on purpose. Connor’s Grok should append or replace this page when given a task that does not yet deserve its own file.

When a note grows up, move it to `docs/connor/<topic>.md`, register it in `docs/pages.json` under section `connor` with id `connor-<topic>`, and link it from [Connor’s area](#connor).
